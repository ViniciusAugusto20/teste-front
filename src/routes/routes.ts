import { Home, Slider } from '../pages';

const routes = [
  {
    path: '/',
    name: 'Home',
    component: Home,
  },
  {
    path: '/slider',
    name: 'Slider',
    component: Slider,
  },
];

export default routes;
